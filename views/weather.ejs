<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather App</title>
    <link rel="stylesheet" type="text/css" href="css/style.css" />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Caveat&family=Lato:wght@300&family=Libre+Baskerville&family=Open+Sans:wght@500&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container" style="height: <%= containerHeight %>">
      <form action="/" method="post">
        <div class="search-box">
          <i class="bx bxs-map"></i>
          <input
            type="text"
            name="cityName"
            id="cityInput"
            placeholder="Enter Location"
            autocomplete="off"
          />
          <button type="submit" class="bx bx-search"></button>
        </div>
      </form>
      <div class="weather-box" style="visibility: <%= foundLocation %>">
        <div class="box">
          <div class="info-weather">
            <div class="weather">
              <p class="location"><%= location %></p>
              <img src="<%= imageSrc %>" />
              <p class="temperature"><%= temperature %><span>°C</span></p>
              <p class="description"><%= cloudsState %></p>
            </div>
          </div>
        </div>
      </div>

      <div class="weather-detials" style="visibility: <%= foundLocation %>">
        <div class="humidity">
          <i class="bx bx-water"></i>
          <div class="text">
            <div class="info-humidity">
              <span><%= humidityPercentage %></span>
            </div>
            <p>Humidity</p>
          </div>
        </div>

        <div class="wind">
          <i class="bx bx-wind"></i>
          <div class="text">
            <div class="info-wind">
              <span><%= windSpeedKM %>Km/h</span>
            </div>
            <p>Wind Speed</p>
          </div>
        </div>
      </div>
      <div class="not-found" style="visibility: <%= notFound %>">
        <div class="box">
          <img src="<%= imageSrc %>" />
          <p>Oops! Location not found!</p>
        </div>
      </div>
    </div>
  </body>
</html>
